{
  "private": true,
  "scripts": {
    "dev": "parcel src/index.html -d dist/development/ --open",
    "build": "parcel build src/index.html -d dist/production/ --no-source-maps --public-url ./",
    "test": "jest"
  },
  "devDependencies": {
    "jest": "^25.1.0",
    "parcel-bundler": "^1.12.4"
  }
}
